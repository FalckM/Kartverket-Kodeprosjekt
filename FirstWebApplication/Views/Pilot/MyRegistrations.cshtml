@model IEnumerable<FirstWebApplication.Entities.ObstacleData>

@{
    ViewData["Title"] = "My Registrations";
    var incompleteQuickRegs = ViewBag.IncompleteQuickRegs as List<FirstWebApplication.Entities.QuickRegistration> ?? new List<FirstWebApplication.Entities.QuickRegistration>();
    var pending = Model.Where(o => !o.IsApproved && !o.IsRejected).ToList();
    var approved = Model.Where(o => o.IsApproved).ToList();
    var rejected = Model.Where(o => o.IsRejected).ToList();
}

<div class="container mx-auto px-4 py-8">
    <div class="mb-6">
        <h1 class="text-3xl font-bold text-gray-900">My Registrations</h1>
        <p class="text-gray-600 mt-2">Manage your obstacle registrations</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
        <div class="bg-yellow-50 border-l-4 border-yellow-500 p-4 rounded">
            <p class="text-yellow-800 font-semibold">Incomplete</p>
            <p class="text-3xl font-bold text-yellow-900">@incompleteQuickRegs.Count</p>
        </div>
        <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded">
            <p class="text-blue-800 font-semibold">Pending Review</p>
            <p class="text-3xl font-bold text-blue-900">@pending.Count</p>
        </div>
        <div class="bg-green-50 border-l-4 border-green-500 p-4 rounded">
            <p class="text-green-800 font-semibold">Approved</p>
            <p class="text-3xl font-bold text-green-900">@approved.Count</p>
        </div>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 rounded">
            <p class="text-red-800 font-semibold">Rejected</p>
            <p class="text-3xl font-bold text-red-900">@rejected.Count</p>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="mb-4 p-4 bg-green-100 border-l-4 border-green-500 text-green-700">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="mb-4 p-4 bg-red-100 border-l-4 border-red-500 text-red-700">
            @TempData["ErrorMessage"]
        </div>
    }

    @if (incompleteQuickRegs.Any())
    {
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-4">⚠️ Incomplete Quick Registrations</h2>
            <p class="text-gray-600 mb-4">You have <strong>@incompleteQuickRegs.Count</strong> incomplete quick registration(s).</p>
            
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-yellow-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Location</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Date</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        @foreach (var quickReg in incompleteQuickRegs)
                        {
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 text-sm font-medium text-gray-900">#@quickReg.Id</td>
                                <td class="px-6 py-4 text-sm text-gray-600">
                                    @{
                                        var coords = quickReg.ObstacleGeometry.Replace("POINT(", "").Replace(")", "").Split(' ');
                                        var displayCoords = coords.Length == 2 ? $"{coords[1]}, {coords[0]}" : "Unknown";
                                    }
                                    📍 @displayCoords
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-600">@quickReg.RegisteredDate.ToString("dd.MM.yyyy HH:mm")</td>
                                <td class="px-6 py-4 text-sm font-medium">
                                    <a asp-action="CompleteQuickRegister" asp-route-id="@quickReg.Id" class="text-blue-600 hover:text-blue-900 mr-4">✏️ Complete</a>
                                    <form asp-action="DeleteQuickRegistration" asp-route-id="@quickReg.Id" method="post" class="inline" onsubmit="return confirm('Delete this registration?');">
                                        <button type="submit" class="text-red-600 hover:text-red-900">🗑️ Delete</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

    @if (pending.Any())
    {
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-4">⏳ Pending Review</h2>
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-blue-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Name</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Height</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Date</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        @foreach (var obstacle in pending)
                        {
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 text-sm font-medium text-gray-900">#@obstacle.Id</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@(obstacle.ObstacleType ?? "N/A")</td>
                                <td class="px-6 py-4 text-sm text-gray-900">@obstacle.ObstacleName</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@obstacle.ObstacleHeight m</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@obstacle.RegisteredDate.ToString("dd.MM.yyyy")</td>
                                <td class="px-6 py-4 text-sm font-medium">
                                    <a asp-action="Overview" asp-route-id="@obstacle.Id" class="text-blue-600 hover:text-blue-900 mr-4">👁️ View</a>
                                    <form asp-action="DeleteRegistration" asp-route-id="@obstacle.Id" method="post" class="inline" onsubmit="return confirm('Delete this registration?');">
                                        <button type="submit" class="text-red-600 hover:text-red-900">🗑️ Delete</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

    @if (approved.Any())
    {
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-4">✅ Approved</h2>
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-green-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Name</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Height</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Approved By</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        @foreach (var obstacle in approved)
                        {
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 text-sm font-medium text-gray-900">#@obstacle.Id</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@(obstacle.ObstacleType ?? "N/A")</td>
                                <td class="px-6 py-4 text-sm text-gray-900">@obstacle.ObstacleName</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@obstacle.ObstacleHeight m</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@obstacle.ApprovedBy</td>
                                <td class="px-6 py-4 text-sm font-medium">
                                    <a asp-action="Overview" asp-route-id="@obstacle.Id" class="text-blue-600 hover:text-blue-900">👁️ View</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

    @if (rejected.Any())
    {
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-4">❌ Rejected</h2>
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-red-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Name</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Rejected By</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Reason</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-700 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        @foreach (var obstacle in rejected)
                        {
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 text-sm font-medium text-gray-900">#@obstacle.Id</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@(obstacle.ObstacleType ?? "N/A")</td>
                                <td class="px-6 py-4 text-sm text-gray-900">@obstacle.ObstacleName</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@obstacle.RejectedBy</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@(string.IsNullOrEmpty(obstacle.RejectionReason) ? "No reason" : obstacle.RejectionReason)</td>
                                <td class="px-6 py-4 text-sm font-medium">
                                    <a asp-action="Overview" asp-route-id="@obstacle.Id" class="text-blue-600 hover:text-blue-900">👁️ View</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

    @if (!incompleteQuickRegs.Any() && !pending.Any() && !approved.Any() && !rejected.Any())
    {
        <div class="bg-gray-50 rounded-lg p-8 text-center">
            <p class="text-gray-600 text-lg mb-4">You haven't registered any obstacles yet.</p>
            <a asp-action="RegisterType" class="inline-block px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
                Register Your First Obstacle
            </a>
        </div>
    }
</div>
